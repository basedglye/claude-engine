# COO review — Cycle 3, Lane 1 (the lobby gets real depth)

Commit under review: `80ab175`.
Reviewed from code, the committed C3-W1 shots, and the verdict JSONs under
`artifacts/verdicts/`. **Evidence provenance:** the browser verdicts I relied
on (`fps-look-interact.json` 00:14, `reserva-readability.json` 00:15,
`save-restore.json` 00:15, `demo-visual.json` 00:15) and the headless ones
(00:13) were written *before* this commit's timestamp (00:16:30) but reflect
this lane's working tree; they are valid for lane 1 and **stale for anything
merged after it** (see C3-W2). I did not build or run anything: the C3-W4
sim/bot lane holds the tree.

**Verdict: PASS with two numbered follow-ups (neither blocking the merge).**

---

## 1. What I verified

**Generator change (`packages/interiors/src/layout.ts`).** `FH` 30→41 base and
`lobbyDepth` 9→20 base. Draw order is unchanged: `FW`, `FH`, `lobbyDepth`,
`corridorW` are still four consecutive `layoutRng.int` calls in the same
sequence, and `Rng.int` consumes exactly one `nextUint32()` regardless of its
range (`packages/core/src/rng.ts:66-68`), so no downstream stream is
perturbed. Invariant 2 (determinism) holds; invariant 3 (replayability) is
honoured by the re-pinned golden rather than papered over.

**Golden (`packages/interiors/scripts/test.mjs`).** `0x752bc750` →
`0x43fd1d2e`, one line, exactly as the ruling required.

**`exterior.ts`.** `rope.castShadow = false` and `fence.castShadow = false`,
both commented with the C2-W2 rationale. Presentation-only, no sim contact.

**Scenario re-derivations.** I checked each of the four scenario diffs for the
thing that actually matters — *did an assertion's meaning move?* It did not:

| Scenario | Assertions | Change |
|---|---|---|
| `fps-look-interact` | 2, verbatim unchanged (`doorIndex 0` open in state; a `door` event with `{doorIndex:0, open:true}`) | walk 9→6 ticks, look `-75`→`-39`, corrective look `+142`→`0` |
| `reserva-readability` | 5, unchanged | `WALK_TICKS` 16→14, `LOOK_DX` -394→-379 |
| `save-restore` | 3, unchanged | comment-only; `WALK_TICKS_1=30` still covers the new tick-13 pin |
| `corridor-headon` | 7, unchanged | fixture goal cells shifted (`cz` 10→16, spawn pair 9/15→15/21) |

The derivation comments are the strongest part of this lane. They name the
script, the seed, the arguments, the intermediate bearings, and — crucially —
the **proof**: `fps-look-interact` records the replayed `{tick:7, type:"door",
payload:{doorIndex:0, open:true}}` event, `reserva-readability` records the
terminal ending at `{station:"frontdesk", focusedBy:"player"}`. That is the
H2b `derive-walk.mjs` pattern applied properly, and it is what turns
"re-derived, never by hand" from a claim into a checkable fact. Both verdicts
show `replayCheck.verified: true` and `hashCheck` incremental == slow
(invariant 6 cross-check green).

**Look pass.** `C3-W1-shots/tier0-lobby-west.png`, `tier0-entrance.png` and
the re-shot `C2-W4-shots/lobby-entrance-t0.png` settle the ruling: the lobby
reads as a room now, not a hallway. There is real floor between the desk and
the entrance wall, the corridor door reads as a destination across a space,
and the tier-0 grubby dressing has room to be seen. The "cramped" excuse is
gone. Ruling satisfied.

## 2. The browser gates' command counts

New counts, read from the verdict JSONs' `replay.commands.length`:

| Gate | Old pin | New | Why |
|---|---|---|---|
| `fps-look-interact` | 12 | **8** | 9→6 `move`, plus the corrective `face` disappears (dx quantises to 0 px, so `player-fps` emits no `face`) |
| `reserva-readability` | 20 | **18** | 16→14 `move` |
| `save-restore` | 69 | **69** | unchanged |
| `demo-visual` | 18 | **18** | unchanged |

Both moves are fully accounted for arithmetically by the re-derived walks, and
both scenarios' assertion sets are byte-identical. **I accept 8 / 18 / 69 / 18
as the new pinned constants.**

They are, however, **not recorded anywhere yet**, which is the whole point of
pinning them. The canonical homes that still say `12 / 20 / 69 / 18` are:
`docs/alpha-loop/BREAKDOWN-2.md:25-26`, `:248` (§4 exit-criterion row 9),
`:283`; `docs/alpha-loop/BREAKDOWN.md:417`; `docs/HANDOFF.md:122`; and the
briefs `C2-W2-polish-carries.md:273-275`, `W2-motel-tier-look.md:282-284`,
`W3-walkthrough-endcard.md:279`, `W4-artifact-packaging.md:271-272`. I can
only write under `reviews/`, so this is a lane task, not something I fixed.

## 3. Fix-list

| # | Sev | Item |
|---|---|---|
| W1-1 | must-do, non-blocking | Record the new pinned counts **8 / 20→18 / 69 / 18** in `BREAKDOWN-2.md` §4 row 9 and §5 item 5, `BREAKDOWN.md:417`, and `docs/HANDOFF.md:122` — with a one-line note that `fps-look-interact`'s corrective look now quantises to 0 px and therefore emits no `face` command, so a future run showing 9 is a real regression signal, not noise. A gate whose pinned count lives only in a review is not pinned. |
| W1-2 | rough edge | `reserva-readability`'s new pose sits **1489 mm from a 1500 mm interact radius** — 11 mm of margin. The *old* derivation explicitly rejected that shape ("N=16 is chosen because the player is against the desk by then… the pose is insensitive to a tick either side rather than balanced on one"), and `derive-walk.mjs` regressed it by taking the **first** hold that works, which is by construction the marginal one. Change the script's selection rule from "first in range" to "first in range whose neighbours N±1 are also in range" (or "deepest inside the radius among the first three that work") and re-derive. The gate will pass either way today; the point is that it should not be one float ulp of engine difference away from flaking. |
| W1-3 | note only | `layout.ts`'s comment claims `restH` "keeps the same 17..26 range it always had". It does not, quite: `lobbyDepth`'s *range* widened (`int(0,3)`→`int(0,4)`), so `restH = FH - lobbyDepth - 1` is now 16..26 where it was 17..26. 16 is still legal (the clamp needs `restH >= 15`), so nothing breaks — but the comment asserts a stability property the change does not have, and comments that are load-bearing for "no seed can produce an invalid floor" must be exactly true. Correct the range, or narrow the draw back to `int(0,3)` and re-pin. |

## 4. Still owed on the merged tree (COO, once C3-W4 is off the tree)

`npm run build`, `tsc --noEmit`, `eslint .`, `check-purity.mjs`, 13 headless
gates with `--verify-replay`, 5 browser gates at the new counts. None of that
can run while the sim/bot lane holds the tree, and none of the verdicts on
disk post-date `e6dec1c`.
