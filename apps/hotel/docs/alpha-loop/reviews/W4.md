# W4 review gate — single-file artifact, pointer-lock fallback, Netlify

Reviewer: COO (integrator). Date 2026-09-02. Checklist:
[BREAKDOWN.md](../BREAKDOWN.md) §5. Brief:
[briefs/W4-artifact-packaging.md](../briefs/W4-artifact-packaging.md).

## Verdict

**FIX-LIST — 3 items, item 1 blocking.**

The artifact itself is genuinely good and I verified its central claim
myself: it is self-contained, it boots, the sim runs, WASD moves the
player, and the four Kenney GLBs and their textures are embedded and
byte-exact. The blocking item is not the artifact — it is `netlify.toml`,
which cannot work as written, and which is the one deliverable here that
nobody would discover was broken until a deploy failed.

## What I ran myself

```
node apps/hotel/scripts/build-artifact.mjs      exit 0
  wrote apps/hotel/dist-artifact/grand-foyer.html
  size = 1.69 MB (1,775,391 bytes)   [limit 16 MB]
  embedded assets = 23 (0.72 MB raw), inlined script/style tags = 1
```

Then I served **only** `apps/hotel/dist-artifact/` over a bare
`node:http` server on port 5207 (killed afterwards) and drove the page in
Playwright — `headless: false`, `channel: "chrome"`,
`args: ["--ignore-gpu-blocklist"]`, 1280x720. Screenshots in
[`W4-shots/`](W4-shots/).

**Network — the claim that matters, confirmed.** Across three separate
full loads (15–25 s each), the complete list of requests the page made
was:

```
http://localhost:5207/grand-foyer.html
```

One request: the document. Zero asset requests, zero CDN requests, zero
font requests. Zero page errors and zero console errors on every load.

**The sim runs.** `window.__WORLDFORGE__` present; `world.tick` advanced
505 → 588 while I held `KeyW`; the player's `pos` moved from the spawn
(x 5375) to x 2575, i.e. WASD and `pointer.lock()/look()` both work
through the artifact exactly as through the dev server.

**The embedded assets are real and reachable.** Fetched from inside the
page through the shim:

| URL | Result |
|---|---|
| `/assets/manifest.json` | 200, `application/json`, 774 B — `characters` only, `textures` 0, `models` 0, `env` absent (the reduced manifest, exactly as specified) |
| `/assets/characters/guest-c.glb` | 200, `model/gltf-binary`, **113,596 B** — byte-identical to the file on disk |
| `/assets/characters/Textures/texture-a.png` | 200, `image/png`, **20,171 B** — byte-identical to disk |

So the lane's one admitted gap — *"NOT visually confirmed: guest
characters rendering"* — is not a defect in W4's work. I could not
photograph a guest either, and I established why: **no guest entity
exists in the sim at these ticks.** I waited to tick 1,972 and the world
held zero `guest` components. That is the tier-0 demand collapse W1 is
being retuned for, not anything to do with the shim. The characters are
provably one `loadCharacter()` call away.

## Fix-list

### 1. BLOCKING — `netlify.toml` cannot build as written

```toml
[build]
  base = "apps/hotel"
  command = "npm run build -w apps/hotel"
  publish = "apps/hotel/dist"
```

Two independent errors, both fatal:

- Netlify runs `command` **from `base`**. Inside `apps/hotel`,
  `npm run build -w apps/hotel` fails: `apps/hotel/package.json` declares
  no workspaces, so there is no `apps/hotel` workspace to select from
  there. The `-w` form only works from the repo root.
- `publish` is resolved **relative to `base`**, so this asks Netlify for
  `apps/hotel/apps/hotel/dist`, which never exists.

Either drop `base` and keep the root-relative command and publish path,
or keep `base = "apps/hotel"` and make the command `npm run build` with
`publish = "dist"` — the first is safer, because the build needs the
root `node_modules` and the workspace packages either way. Whichever you
pick, say in a comment which directory the command runs in, since that is
the fact that makes the other two lines correct or wrong.

### 2. Non-blocking — the shim falls through instead of sealing

The `fetch` shim serves embedded `/assets/**` paths from its map and
**passes everything else to the real `fetch`**. I proved this by asking
the page for `/assets/textures/lobby-floor/color.jpg`: the request left
the page and hit my server. In today's artifact nothing ever asks for
such a path (the reduced manifest omits every texture and model), so the
observed request count is genuinely zero — but "no external fetches" is
currently an arrangement rather than an invariant. One added code path,
one changed manifest, or one glTF with an unexpected relative reference
and the artifact starts phoning home with no warning.

Make the shim return a synthetic `404` for any unmatched `/assets/**`
path rather than delegating. Keep the delegation for everything outside
`/assets/`. That turns the guarantee into something the shim enforces.

### 3. Non-blocking — the freshness precondition is existence-only

The header comment is honest — it says the script requires `dist/` to be
fresh and only fails when it is *missing*. But a stale `dist/` from an
earlier session builds a stale artifact silently, and this project has
already been bitten four times by exactly that class of trap (`tsc`
incremental skipping a rebuild). Compare the newest mtime under
`apps/hotel/src/` against `dist/index.html` and refuse when `src` is
newer.

Related, and recorded rather than charged to this lane: **the artifact I
built does not contain the hover-look fallback at all.** I checked —
`pointer-fallback.ts` is not in the bundle, because `main.ts` never
imports it, because that wiring is orchestrator-only. So the lane's
"verified in isolation with `requestPointerLock` mocked to reject" is the
only evidence that exists today, and the fallback gets its real test
after integration. I read the module: canvas-scoped listeners only, no
capture-phase `document` handlers, rAF-throttled emission, an
`isLocked()` guard at the top of both handlers, and a two-frame
post-request check before entering fallback mode. It matches the contract
in the brief. It just is not in the shipped file yet.

## Also confirmed

- Determinism: the emitted HTML carries no timestamp and no random id; a
  second run against the same `dist/` produced the same size and asset
  count.
- Size assertion has real headroom: 1.69 MB against a 16 MB ceiling, and
  the payload split (0.72 MB of assets vs ~1 MB of code) is what the
  brief predicted.
- The lane's own perturbations (size limit lowered to 1 KB;
  self-containment check with one asset left unembedded) are the right
  two to have chosen. I did not re-run them and say so.

## Re-submission

Item 1 only, plus item 2 if it is cheap in the same pass. Re-run
`build-artifact.mjs` and paste the size line. Item 3 is a nice-to-have.
The fallback and the guest-character render both get re-checked by me on
the integrated build, not by this lane.

---

## Round 2 — re-review after the fix-list (COO, 2026-09-02)

**Verdict: PASS.** All three fix-list items are closed. Every claim below
was re-derived by me from a fresh run, not read out of the lane's report.

### 1 (was BLOCKING) — `netlify.toml`: FIXED

`apps/hotel/netlify.toml` now carries no `base`. `command = "npm run build
-w apps/hotel"`, `publish = "apps/hotel/dist"`, and a comment that states
explicitly which directory the command runs in (the repo root, Netlify's
default base) and why `-w` only resolves from there. That is exactly the
safer of the two options I offered, with the fact that makes the other two
lines correct written down beside them.

Proved rather than assumed: from the **repo root**, `npm run build -w
apps/hotel` exits 0 (`tsc -p tsconfig.game.json` then `vite build`, 353
modules), and `apps/hotel/dist/` afterwards contains `index.html` (586 B)
and `assets/index-BE0fKhIJ.js`. The publish path names a directory that
the command actually produces, from the directory the command actually
runs in.

### 2 (was non-blocking) — the shim is sealed: FIXED

`build-artifact.mjs` now answers any unmatched path under `/assets/` with
a synthetic `new Response(null, { status: 404, statusText: "Not Found
(sealed /assets/ shim)" })` instead of delegating; delegation is retained
for everything outside `/assets/`.

Verified in the running page, which is the only place this claim means
anything. I served **only** the artifact's own output directory (one file
in it, nothing else) on port 5207 from a bare `node:http` server that logs
every hit, and drove it in Playwright — `headless: false`, `channel:
"chrome"`, `args: ["--ignore-gpu-blocklist"]`, 1280x720 — logging every
request the page made.

```
requests total = 1, non-data = 1
  http://localhost:5207/grand-foyer.html
server log: SERVER-HIT-200 /grand-foyer.html   (the only hit, ever)
page errors = 0, console errors = 0
world.tick = 285 after ~12 s
```

Exactly one non-data request: the document. Then, from inside the page:

```
await fetch("/assets/textures/lobby-floor/color.jpg")
  -> { ok: false, status: 404, statusText: "Not Found (sealed /assets/ shim)" }
new requests recorded after the probe: []   (and no new server hit)
```

This is the same probe that escaped to my server in round 1. It no longer
leaves the page. The guarantee is now enforced by the shim rather than
being an arrangement that happens to hold.

### 3 (was nice-to-have) — freshness check: FIXED, and it fails loudly

The script walks `apps/hotel/src/` for the newest mtime and compares it
against `dist/index.html`. I perturbed it deliberately: `touch
apps/hotel/src/main.ts`, then re-ran.

```
build-artifact: apps/hotel/dist/ is stale: a file under apps/hotel/src/ is
newer (mtime 2026-09-02T23:42:59.974Z) than dist/index.html (mtime
2026-09-02T23:42:46.649Z). Run "npm run build -w apps/hotel" again, then
re-run this script.
exit 1, and no output file was written
```

Refuses, names both mtimes, names the command to run, and does not leave a
stale artifact behind. After rebuilding, the script succeeds again. That
closes the `tsc`-incremental class of trap this project has been bitten by
four times.

### `--out` and size

`--out <path>` is present, resolves the path, and errors when the argument
is missing. Run to a scratch path under `artifacts/`:

```
node apps/hotel/scripts/build-artifact.mjs --out artifacts/<scratch>/grand-foyer.html
  wrote .../grand-foyer.html
  size = 1.72 MB (1,801,267 bytes)      [limit 16 MB]
  embedded assets = 23 (0.72 MB raw)
  inlined script/style tags = 1
```

1.72 MB confirmed, matching the lane's report to the byte, and nothing was
written outside the directory I named. Scratch output and the temporary
server directory were removed; port 5207 has no listener (TIME_WAIT only).

### What I did NOT re-check this round

I did not re-run the round-1 asset byte-equality checks (the GLB and PNG
comparisons), the determinism double-build, or the lane's own two
perturbations — none of them are touched by these three fixes, and they
passed in round 1. The hover-look fallback is still not in the shipped
artifact, because `main.ts` does not import it yet and that wiring is
orchestrator-only; it is my item on integration, not this lane's. Guest
characters still cannot be photographed for the round-1 reason (no guest
entity exists at these ticks) — a W1 economy matter, not a W4 defect.

**W4 is done.**
