{
  "name": "@claude-engine/hotel",
  "version": "0.0.1",
  "description": "ClaudeEngine hotel game — Phase H0 'Walk the Lobby'.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:game": "tsc -p tsconfig.game.json",
    "build": "npm run build:game && vite build",
    "test": "npm run build:game --silent && node scripts/test.mjs"
  },
  "dependencies": {
    "@claude-engine/assets": "*",
    "@claude-engine/audio": "*",
    "@claude-engine/core": "*",
    "@claude-engine/interiors": "*",
    "@claude-engine/persistence": "*",
    "@claude-engine/player-fps": "*",
    "@claude-engine/renderer-three": "*",
    "@claude-engine/save-web": "*",
    "@claude-engine/space": "*",
    "@claude-engine/surface-ui": "*",
    "three": "^0.169.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
