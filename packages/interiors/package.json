{
  "name": "@claude-engine/interiors",
  "version": "0.0.1",
  "description": "Deterministic procedural interiors: BSP floorplans emitting geometry, nav grid and portal graph from one source of truth",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build --silent && node scripts/test.mjs"
  },
  "dependencies": {
    "@claude-engine/core": "*",
    "@claude-engine/space": "*",
    "@claude-engine/assets": "*"
  }
}
