{
  "name": "@claude-engine/save-web",
  "version": "0.0.1",
  "description": "Browser save/load: IndexedDB GameStore plus a write-ahead save pump, implementing the engine's existing persistence contract",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build --silent && node scripts/test.mjs"
  },
  "dependencies": {
    "@claude-engine/core": "*",
    "@claude-engine/persistence": "*"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0"
  }
}
